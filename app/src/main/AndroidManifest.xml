<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- No-root standalone mode: wake the screen on plug and launch via full-screen intent. -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <!-- Persistent monitor service that survives EMUI background freezing. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- "Display over other apps": on EMUI this also exempts the app from background-activity-start
         blocking, which is required to show the HUD from the monitor service. -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
        android:name=".EndfieldApp"
        android:label="@string/app_name"
        android:description="@string/xposed_description"
        android:supportsRtl="true">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.DeviceDefault.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- No-root standalone charge HUD: translucent, shown over the keyguard. -->
        <activity
            android:name=".standalone.StandaloneChargeActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:launchMode="singleTop"
            android:noHistory="true"
            android:showWhenLocked="true"
            android:taskAffinity=""
            android:theme="@style/Theme.EndfieldCharge.Standalone"
            android:turnScreenOn="true" />

        <!-- Protected system broadcasts, deliverable to manifest receivers (best-effort fallback). -->
        <receiver
            android:name=".standalone.ChargeEventReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.ACTION_POWER_CONNECTED" />
                <action android:name="android.intent.action.ACTION_POWER_DISCONNECTED" />
            </intent-filter>
        </receiver>

        <!-- Persistent foreground monitor: keeps the dynamic charger receiver alive while enabled. -->
        <service
            android:name=".standalone.StandaloneMonitorService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Detects charger plug/unplug to play a custom charging animation" />
        </service>

        <!-- Restart the monitor after reboot / Huawei quick boot. -->
        <receiver
            android:name=".standalone.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="huawei.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <provider
            android:name=".settings.SettingsProvider"
            android:authorities="${applicationId}.settings"
            android:exported="true" />

    </application>

</manifest>
